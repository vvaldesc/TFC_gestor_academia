---
import { extractEmotionCssAndJs } from './Extract_html';

export interface Props {
  component: () => React.ReactNode;
}
console.log("hola")

const component = Astro.props.component;
const content = extractEmotionCssAndJs(component);
---

<Fragment set:html={content} ></Fragment>
<!-- <ExtractEmotionCss client:load /> -->