---
import { createContext } from '@astropub/context'
import type {sessionInfoState} from "@/models/types"; // prettier-ignore

export const [ Provider, getContext ] = createContext<{ sessionInfoState: sessionInfoState }>();
---